--문제1.
SELECT RPAD(SUBSTR(E.EMPNO,1,2),LENGTH(E.EMPNO),'*') EMPNO
     , E.ENAME
  FROM (SELECT * FROM EMP ORDER BY EMPNO DESC) E
;
--문제2.
SELECT E.EMPNO
     , E.ENAME
     , D.DNAME
     , D.LOC
  FROM EMP E
     , DEPT D
 WHERE E.DEPTNO = D.DEPTNO
 ORDER BY D.DNAME DESC
;